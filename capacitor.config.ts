import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.divine.adminhub',
  appName: 'Sri Ramalayam Admin Hub',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
